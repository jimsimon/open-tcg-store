import type { ProductSearchResultResolvers } from './../../types.generated';
export const ProductSearchResult: ProductSearchResultResolvers = {
  /* Passthrough – the service layer already returns the correct shape */
};
