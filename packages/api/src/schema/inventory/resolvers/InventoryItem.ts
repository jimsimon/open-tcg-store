import type { InventoryItemResolvers } from "./../../types.generated";
export const InventoryItem: InventoryItemResolvers = {
  /* Passthrough – the service layer already returns the correct shape */
};
