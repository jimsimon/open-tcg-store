local_resource(
    "UI Server",
    serve_cmd="pnpm --filter @open-tcgs/ui run dev",
    resource_deps=[]
)

local_resource(
    "API Server",
    serve_cmd="pnpm --filter @open-tcgs/api run dev",
    resource_deps=[]
)
